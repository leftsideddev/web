[build]
  command = "npm run build"
  publish = "dist"
  functions = "functions"

# Handle SPA routing: redirect all requests to index.html
# This allows React Router to handle the URL paths directly.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200